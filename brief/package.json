{
  "name": "nova-brief",
  "private": true,
  "version": "1.0.0",
  "description": "Nova Brief — Nova Social's marketing publication. Static production build, independently deployable to brief.novasocial.co.uk.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/generate-articles.mjs",
    "preview": "vite preview --port 4174"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3"
  }
}
